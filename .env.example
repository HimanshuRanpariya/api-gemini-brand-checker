# Example environment variables for Gemini Brand Mention Checker (server)
# Copy this file to `.env` and fill in values. Do NOT commit your real `.env`.

# Your Gemini-style API key (server-side only)
GEMINI_API_KEY=

# Full URL to your model responses endpoint (e.g. OpenAI Responses API or Gemini endpoint)
# Example for OpenAI: https://api.openai.com/v1/responses
GEMINI_API_HOST=

# Fixed model and temperature (optional overrides, not recommended)
GEMINI_MODEL=gpt-4o-mini
GEMINI_TEMPERATURE=0.2

# If you only have a Google "Gemini" / Generative API key, the server will try
# to call Google Generative API automatically when `GEMINI_API_HOST` is not set.
# Default Google model (override with GEMINI_MODEL_GOOGLE):
GEMINI_MODEL_GOOGLE=models/text-bison-001

# Port the Express server should listen on
PORT=5000
